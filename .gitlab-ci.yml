# LGDXRobot Cloud GitLab CI/CD
# Variables
# $EXT_REGISTRY: Registry URL (e.g. docker.io)
# $EXT_USER: Username for the registry
# $EXT_PAT: PAT for the registry (https://docs.docker.com/security/for-developers/access-tokens/)

variables:
  # 1) Name of directory where restore and build objects are stored.
  OBJECTS_DIRECTORY: 'obj'
  # 2) Name of directory used for keeping restored dependencies.
  NUGET_PACKAGES_DIRECTORY: '.nuget'
  # 3) A relative path to the source code from project repository root.
  SOURCE_CODE_PATH: '*/'
  GITLAB_REG_IMAGE_NAME: $CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG

# To configure cache to pass over restored dependencies for re-use.
cache:
  # Per-stage and per-branch caching.
  key: "$CI_JOB_STAGE-$CI_COMMIT_REF_SLUG"
  paths:
    # 1) Main JSON file holding information about package dependency tree, packages versions,
    # frameworks etc. It also holds information where to the dependencies were restored.
    - '$SOURCE_CODE_PATH$OBJECTS_DIRECTORY/project.assets.json'
    # 2) Other NuGet and MSBuild related files. Also needed.
    - '$SOURCE_CODE_PATH$OBJECTS_DIRECTORY/*.csproj.nuget.*'
    # 3) Path to the directory where restored dependencies are kept.
    - '$NUGET_PACKAGES_DIRECTORY'
  # 'pull-push' policy means that latest cache will be downloaded (if it exists)
  policy: pull-push

stages:
  - build-2
  - release

# Build Docker Images for AMD64
release-docker-amd64:
  image: docker:24.0.5-git
  stage: build-2
  dependencies: []
  services:
    - docker:24.0.5-dind
  script:
    - docker build -t $CI_REGISTRY_IMAGE/api-amd64:latest -f Dockerfile.api .
    - docker build -t $CI_REGISTRY_IMAGE/ui-amd64:latest -f Dockerfile.ui .
    - docker build -t $CI_REGISTRY_IMAGE/worker-amd64:latest -f Dockerfile.worker .
    - echo -n $CI_REGISTRY_PASSWORD | docker login -u $CI_REGISTRY_USER --password-stdin $CI_REGISTRY
    - docker push $CI_REGISTRY_IMAGE/api-amd64:latest
    - docker push $CI_REGISTRY_IMAGE/ui-amd64:latest
    - docker push $CI_REGISTRY_IMAGE/worker-amd64:latest
  only:
    - main

# Build Docker Images for ARM64
release-docker-arm64:
  image: docker:24.0.5-git
  stage: build-2
  dependencies: []
  tags:
    - saas-linux-small-arm64 
  services:
    - docker:24.0.5-dind
  script:
    - docker build -t $CI_REGISTRY_IMAGE/api-arm64:latest -f Dockerfile.api .
    - docker build -t $CI_REGISTRY_IMAGE/ui-arm64:latest -f Dockerfile.ui .
    - docker build -t $CI_REGISTRY_IMAGE/worker-arm64:latest -f Dockerfile.worker .
    - echo -n $CI_REGISTRY_PASSWORD | docker login -u $CI_REGISTRY_USER --password-stdin $CI_REGISTRY
    - docker push $CI_REGISTRY_IMAGE/api-arm64:latest
    - docker push $CI_REGISTRY_IMAGE/ui-arm64:latest
    - docker push $CI_REGISTRY_IMAGE/worker-arm64:latest
  only:
    - main

# Combine Docker Images and Push to Registry
release-docker-push:
  image: docker:24.0.5-git
  stage: release
  dependencies: []
  services:
    - docker:24.0.5-dind
  script:
    # Get version
    - apk add libxml2-utils
    - TEMP_TAG=$(xmllint --xpath "//Project/PropertyGroup/Version/text()" version.xml)
    - echo -n $CI_REGISTRY_PASSWORD | docker login -u $CI_REGISTRY_USER --password-stdin $CI_REGISTRY
    # Combine images
    - docker pull $CI_REGISTRY_IMAGE/api-amd64:latest
    - docker pull $CI_REGISTRY_IMAGE/api-arm64:latest
    - docker manifest create $EXT_REGISTRY/$EXT_USER/lgdxrobotcloud.api:latest \
        $CI_REGISTRY_IMAGE/api-amd64:latest \
        $CI_REGISTRY_IMAGE/api-arm64:latest
    - docker manifest create $EXT_REGISTRY/$EXT_USER/lgdxrobotcloud.api:$TEMP_TAG \
        $CI_REGISTRY_IMAGE/api-amd64:latest \
        $CI_REGISTRY_IMAGE/api-arm64:latest
    - docker pull $CI_REGISTRY_IMAGE/ui-amd64:latest
    - docker pull $CI_REGISTRY_IMAGE/ui-arm64:latest
    - docker manifest create $EXT_REGISTRY/$EXT_USER/lgdxrobotcloud.ui:latest \
        $CI_REGISTRY_IMAGE/ui-amd64:latest \
        $CI_REGISTRY_IMAGE/ui-arm64:latest
    - docker manifest create $EXT_REGISTRY/$EXT_USER/lgdxrobotcloud.ui:$TEMP_TAG \
        $CI_REGISTRY_IMAGE/ui-amd64:latest \
        $CI_REGISTRY_IMAGE/ui-arm64:latest
    - docker pull $CI_REGISTRY_IMAGE/worker-amd64:latest
    - docker pull $CI_REGISTRY_IMAGE/worker-arm64:latest
    - docker manifest create $EXT_REGISTRY/$EXT_USER/lgdxrobotcloud.worker:latest \
        $CI_REGISTRY_IMAGE/worker-amd64:latest \
        $CI_REGISTRY_IMAGE/worker-arm64:latest
    - docker manifest create $EXT_REGISTRY/$EXT_USER/lgdxrobotcloud.worker:$TEMP_TAG \
        $CI_REGISTRY_IMAGE/worker-amd64:latest \
        $CI_REGISTRY_IMAGE/worker-arm64:latest
    # Push images
    - docker logout $CI_REGISTRY
    - echo -n $EXT_PAT | docker login -u $EXT_USER --password-stdin $EXT_REGISTRY
    - docker manifest push $EXT_REGISTRY/$EXT_USER/lgdxrobotcloud.api:latest
    - docker manifest push $EXT_REGISTRY/$EXT_USER/lgdxrobotcloud.api:$TEMP_TAG
    - docker manifest push $EXT_REGISTRY/$EXT_USER/lgdxrobotcloud.ui:latest
    - docker manifest push $EXT_REGISTRY/$EXT_USER/lgdxrobotcloud.ui:$TEMP_TAG
    - docker manifest push $EXT_REGISTRY/$EXT_USER/lgdxrobotcloud.worker:latest
    - docker manifest push $EXT_REGISTRY/$EXT_USER/lgdxrobotcloud.worker:$TEMP_TAG
  only:
    - main