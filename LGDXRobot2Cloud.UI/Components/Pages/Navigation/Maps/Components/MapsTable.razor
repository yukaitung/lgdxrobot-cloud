@inherits AbstractTable

<div class="card">
  <!-- Header -->
  <div class="card-header">
    <h3 class="card-title">Maps</h3>
  </div>
  <!-- Filter -->
  <div class="card-body border-bottom py-3">
    <div class="d-flex">
      <div class="text-secondary">
        <div class="row g-2">
          <div class="col">
            Show
          </div>
          <div class="col-auto">
            <TablePageSizeSelect OnChange="HandlePageSizeChange" />
          </div>
          <div class="col-auto">
            entries
          </div>
        </div>
      </div>
      <div class="ms-auto text-secondary">
        Search:
      </div>
      <div class="text-secondary">
        <div class="input-group">
          <input type="text" class="form-control form-control-sm ms-2" @bind="DataSearch">
          <button type="button" class="btn btn-sm" @onclick="HandleSearch">Search</button>
          <button type="button" class="btn btn-sm" @onclick="HandleClearSearch">Clear</button>
        </div>
      </div>
    </div>
  </div>

  <!-- Data -->
  <div class="table-responsive">
    <table class="table card-table table-vcenter text-nowrap datatable">
      <thead>
        <tr>
          <th class="w-1">ID</th>
          <th>Name</th>
          <th>Description</th>
          <th class="w-1">Resolution</th>
          <th class="w-1">Create Time</th>
          <th class="w-1">Last Update Time</th>
          <th class="w-1"><!-- Action --></th>
        </tr>
      </thead>
      <tbody>
        @if (MapsList != null)
        {
          @foreach (var Map in MapsList)
          {
            var mapId = Map.Id;
            <tr>
              <td>@Map.Id</td>
              <td>@Map.Name</td>
              <td>@Map.Description</td>
              <td>@Map.Resolution</td>
              <td>@Map.CreatedAt</td>
              <td>@Map.UpdatedAt</td>
              <td class="text-end">
                <a href="@(AppRoutes.Navigation.Maps.Index + $"/{mapId}")" class="page-link link-primary">Edit</a>
              </td>
            </tr>
          }
        }
        else
        {
          <TableLoading Columns="6" />
        }
      </tbody>
    </table>
    @if (MapsList != null && MapsList.Count() == 0)
    {
      <TableEmpty />
    }
  </div>
  <!-- Footer -->
  @if (PaginationHelper != null && MapsList != null && MapsList.Count() > 0)
  {
    <TablePagination PaginationHelper="PaginationHelper" ItemCount="MapsList.Count()"
      OnPageChange="HandlePageChange" />
  }
</div>
