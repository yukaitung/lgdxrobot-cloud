<div class="card h-100">
  <!-- Header -->
  <div class="card-header">
    <h3 class="card-title">Assigned Tasks</h3>
  </div>

  <!-- Data -->
  <div class="table-responsive">
    <table class="table card-table table-vcenter text-nowrap datatable">
      <thead>
        <tr>
          <th class="w-1">ID</th>
          <th>Name</th>
          <th>Priority</th>
          <th>Current Progress</th>
          <th>Flow</th>
        </tr>
      </thead>
      <tbody>
        @if (AutoTasks != null)
        {
          @foreach (var autoTask in AutoTasks)
          {
            <tr>
              <td>@autoTask.Id</td>
              <td>@autoTask.Name</td>
              <td>@autoTask.Priority</td>
              <td>@autoTask.CurrentProgress.Name</td>
              <td>@autoTask.Flow.Name</td>
            </tr>
          }
        }
        else
        {
          <TableLoading Columns="7" />
        }
      </tbody>
    </table>
    @if (AutoTasks != null && AutoTasks.Count() == 0)
    {
      <div class="empty h-25">
        <!--<p class="empty-title">No results found</p>-->
        <p class="empty-subtitle text-secondary">
          The robot currently does not has assigned task.
        </p>
      </div>
    }
  </div>
</div>
