<div class="modal modal-blur fade" id="nodeDetailModal" data-bs-backdrop="static" data-bs-keyboard="false"
  tabindex="-1">
  <div class="modal-dialog modal-lg modal-dialog-centered" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">@(Id != null ? "Update Node" : "Create New Node")</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <EditForm EditContext="_editContext" OnValidSubmit="HandleValidSubmit" OnInvalidSubmit="HandleInvalidSubmit">
        <DataAnnotationsValidator />
        <div class="modal-body">
          <ModalValidationError Display="_isInvalid" />
          <ModalSubmissionError Display="_isError" />
          @if (Id != null)
          {
            <div class="mb-3 row">
              <label class="col-3 col-form-label">ID</label>
              <div class="col ms-3 form-control-plaintext">@_node.Id</div>
            </div>
          }
          <div class="mb-3 row">
            <label class="col-3 col-form-label required" for="Name">Name</label>
            <div class="col">
              <InputText id="Name" @bind-Value="_node.Name" type="text" class="form-control"
                placeholder="Your node name" />
              <ValidationMessage class="invalid-feedback" For="@(() => _node.Name)" />
            </div>
          </div>
          <div class="mb-3 row">
            <label class="col-3 col-form-label required" for="ProcessName">Process Name</label>
            <div class="col">
              <InputText id="ProcessName" @bind-Value="_node.ProcessName" type="text" class="form-control"
                placeholder="e.g. ros2 launch" />
              <ValidationMessage class="invalid-feedback" For="@(() => _node.ProcessName)" />
            </div>
          </div>
          <div class="mb-3 row">
            <label class="col-3 col-form-label" for="Arguments">Arguments</label>
            <div class="col">
              <InputTextArea id="Arguments" @bind-Value="_node.Arguments" class="form-control" rows="3"
                placeholder="e.g. lgdxrobot2_navigation nav2_rtabmap.launch.py"></InputTextArea>
              <ValidationMessage class="invalid-feedback" For="@(() => _node.Arguments)" />
            </div>
          </div>
          @if (Id != null)
          {
            <div class="mb-3 row">
              <label class="col-3 col-form-label">Create Time</label>
              <div class="col ms-3 form-control-plaintext">@_node.CreatedAt</div>
            </div>
            <div class="mb-3 row">
              <label class="col-3 col-form-label">Last Update Time</label>
              <div class="col ms-3 form-control-plaintext">@_node.UpdatedAt</div>
            </div>
          }
        </div>
        <div class="modal-footer">
          <a class="btn btn-link link-secondary" data-bs-dismiss="modal">
            Cancel
          </a>
          @if (Id != null)
          {
            <button class="btn btn-danger ms-auto" data-bs-toggle="modal" data-bs-target="#nodeDeleteModal">
              Delete Node
            </button>
            <button type="submit" class="btn btn-primary ms-2">
              Update Node
            </button>
          }
          else 
          {
            <button type="submit" class="btn btn-primary ms-auto">
              Create New Node
            </button>
          }
        </div>
      </EditForm>
    </div>
  </div>
</div>

@if (Id != null)
{
  <div class="modal fade" id="nodeDeleteModal" tabindex="-1" data-bs-keyboard="false">
    <div class="modal-dialog modal-sm modal-dialog-centered" role="document">
      <div class="modal-content">
        <div class="modal-body">
          <div class="modal-title">Delete Node</div>
          <ModalDeleteError Display="_isDeleteError" />
          <div>The Node @(_node.Name != null ? $"\"{_node.Name}\" (ID: {_node.Id})" : $"ID: {_node.Id}") will be deleted.</div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-link link-secondary me-auto" data-bs-dismiss="modal">Cancel</button>
          <button type="button" class="btn btn-danger" @onclick="HandleDelete">Yes</button>
        </div>
      </div>
    </div>
  </div>
}